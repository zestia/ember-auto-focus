import { autoFocus } from 'dummy/helpers/auto-focus';
import { module, test } from 'qunit';

module('Unit | Helper | auto focus');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = autoFocus([42]);
  assert.ok(result);
});
